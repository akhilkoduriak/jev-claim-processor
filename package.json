{
  "name": "smart-insurance-claim-processor",
  "version": "1.0.0",
  "description": "Insurance claim triage console using TypeSafe AI's Jev model, with rule-based fallback",
  "main": "backend/server.js",
  "scripts": {
    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
    "backend": "cd backend && node server.js",
    "frontend": "cd frontend && npm start",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "test": "cd backend && npm test"
  },
  "keywords": [
    "insurance",
    "claims",
    "jev",
    "real-time",
    "anomaly-detection"
  ],
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "private": true
}
